import { FallbackimagesDirective } from './fallbackimages.directive';

describe('FallbackimagesDirective', () => {
  it('should create an instance', () => {
    const directive = new FallbackimagesDirective();
    expect(directive).toBeTruthy();
  });
});
